<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create(config('sabhero-estimator.table.prefix') . 'surfaces', function (Blueprint $table) {
            $table->id();
            $table->foreignId('area_id')->constrained(config('sabhero-estimator.table.prefix') . 'areas')->onDelete('cascade');
            $table->string('surface_type');
            $table->decimal('measurement', 10, 2)->nullable();
            $table->integer('quantity')->default(1);
            $table->timestamps();

            $table->index('area_id');
            $table->index('surface_type');
        });
    }

    public function down(): void
    {
        Schema::dropIfExists(config('sabhero-estimator.table.prefix') . 'surfaces');
    }
};